name: amoi
description: Amoi groupe by Bô studio MADAGASCAR
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=2.18.0-271.2.beta <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_native_splash: ^2.2.6
  carousel_slider: ^4.1.1
  smooth_page_indicator: ^1.0.0+2
  firebase_core: ^2.3.0
  cloud_firestore: ^4.1.0
  flutter_easyloading: ^3.0.5
  shared_preferences: ^2.0.15
  connectivity_plus: ^3.0.2
  flutter_styled_toast: ^2.1.3
  intl: ^0.17.0
  hidden_drawer_menu: ^3.0.1
  flutter_svg: ^1.1.6
  firebase_storage: ^11.0.10
  image_picker: ^0.8.6+1
  video_player: ^2.5.1
  toast: ^0.3.0

  
flutter_native_splash:
  image: assets/logo/logosplashscreen.png
  color: "#000000"  # bleu -> #42a5f5
  fullscreen: true

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  

flutter:
  uses-material-design: true
  assets:
  - assets/
  - assets/logo/
  - assets/money/
